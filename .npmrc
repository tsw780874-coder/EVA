# EVA Project — Keep all caches inside the project directory (D drive).
# This prevents npm from writing to %APPDATA%\npm-cache on C drive.
cache=.cache/npm

# Disable npm telemetry
audit=false
fund=false
update-notifier=false
